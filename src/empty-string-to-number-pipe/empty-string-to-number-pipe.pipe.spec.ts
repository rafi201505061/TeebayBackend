import { EmptyStringToNumberPipePipe } from './empty-string-to-number-pipe.pipe';

describe('EmptyStringToNumberPipePipe', () => {
  it('should be defined', () => {
    expect(new EmptyStringToNumberPipePipe()).toBeDefined();
  });
});
